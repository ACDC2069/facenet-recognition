# 环境变量配置示例
# 请将此文件复制为 .env 并填写实际值

# 加密密钥（32字节，用于AES-256加密）
# 生成方法: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# 应用配置
APP_HOST=0.0.0.0
APP_PORT=8000

# 人脸识别阈值（欧氏距离，越小越严格）
FACE_RECOGNITION_THRESHOLD=0.6

